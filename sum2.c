#include <stdio.h>
    int sum(int a,int b,int c);
    
    {
        printf("%d",(a+b+c));
        return o;
    }
    int main()
    {
        int a,b,c;
        printf("enter the numbers:");
        scanf("%d%d%d",&a,&b,&c);
        sum(a,b,c);
        return 0;
    }